{
  "version": 2,
  "buildCommand": "node -e \"const {execSync} = require('child_process'), fs = require('fs'); if (fs.existsSync('frontend')) { execSync('cd frontend && npm run build', {stdio:'inherit'}); } else { execSync('npm run build', {stdio:'inherit'}); }\"",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
